---
name: "react-native-expo-cursorrules-prompt-file"
clean_name: "React Native Expo"
description: "Cursor rules for React Native Expo development."
description_tr: "React Native Expo geliştirmesi için Cursor kuralları."
category: "Mobile"
repo: "PatrickJS/awesome-cursorrules"
stars: 39709
path: "rules/react-native-expo-cursorrules-prompt-file.mdc"
url: "https://github.com/PatrickJS/awesome-cursorrules/blob/main/rules/react-native-expo-cursorrules-prompt-file.mdc"
body_length: 997
file_extension: ".mdc"
body_tr: |-
  ```
  // React Native Expo .cursorrules

  // React Native Expo en iyi uygulamaları

  const reactNativeExpoBestPractices = [
    "Fonksiyonel komponentleri hooks ile kullanın",
    "Expo SDK özelliklerini ve API'lerini kullanın",
    "Expo Router kullanarak uygun navigasyonu uygulayın",
    "Görüntüler ve fontlar için Expo'nun asset sistemini kullanın",
    "Uygun hata işlemesi ve crash raporlaması uygulayın",
    "Expo'nun push notification sistemini kullanın",
  ];

  // Klasör yapısı

  const folderStructure = `
  assets/
  src/
    components/
    screens/
    navigation/
    hooks/
    utils/
  App.js
  app.json
  `;

  // Ek talimatlar

  const additionalInstructions = `
  1. Tip güvenliği için TypeScript kullanın
  2. StyleSheet kullanarak uygun stili uygulayın
  3. Expo'nun vector ikonlarını kullanın
  4. Hassas veriler için Expo'nun secure store'unu kullanın
  5. Uygun çevrimdışı destek uygulayın
  6. Performans için React Native en iyi uygulamalarını izleyin
  7. Hızlı dağıtımlar için Expo'nun OTA güncellemelerini kullanın
  `;
  ```
---

// React Native Expo .cursorrules

// React Native Expo best practices

const reactNativeExpoBestPractices = [
  "Use functional components with hooks",
  "Utilize Expo SDK features and APIs",
  "Implement proper navigation using Expo Router",
  "Use Expo's asset system for images and fonts",
  "Implement proper error handling and crash reporting",
  "Utilize Expo's push notification system",
];

// Folder structure

const folderStructure = `
assets/
src/
  components/
  screens/
  navigation/
  hooks/
  utils/
App.js
app.json
`;

// Additional instructions

const additionalInstructions = `
1. Use TypeScript for type safety
2. Implement proper styling using StyleSheet
3. Utilize Expo's vector icons
4. Use Expo's secure store for sensitive data
5. Implement proper offline support
6. Follow React Native best practices for performance
7. Use Expo's OTA updates for quick deployments
`;
